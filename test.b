
cons = ba[];
cons 77;
mac(x,c) = (x*c+3353);

a 10;
var;word "hi";
words[] /* words*/ "Hello", "World!";
arr[20];
barr$[20]; // and this
seq$[] 1, 3, 5, 10, 11;
static b 17;

include "lib.b";

f(x) {
    auto a[10], b$[16+1];
    static a 10;
    if(a[1*1] == 10) b[17];
    else a[1]--;
}

